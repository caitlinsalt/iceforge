import { describe, test } from 'vitest';

describe('loadTemplates() tests', () => {
    test.todo('Returns template for each file in environment templates folder');
    test.todo('Loads correct template plugin for each file');
    test.todo('Ignores files that do not have a matching plugin');
    test.todo('Throws error if plugin fromFile() function throws error');
});
